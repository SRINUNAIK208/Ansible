- name: install packages
  hosts: node
  become: yes
  tasks:
  -  name: packages
     ansible.builtin.package:
      name: "{{ item }}"
      state: present
     loop:
     - mysql
     - nginx
     - git
